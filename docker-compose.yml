version: "3.3"

services:
  api:
    build: .
    environment:
      NODE_ENV: production
    ports:
      - 4000:4000
    networks:
      - c_clicker_api

  db:
    image: postgres
    env-file:
      - .env.db.prod
    networks:
      - c_clicker_api
networks:
  c_clicker_api:
    driver: bridge
