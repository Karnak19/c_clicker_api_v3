version: "2.1"

services:
  c_clicker_express_seq:
    image: c_clicker_express_seq
    build: .
    environment:
      NODE_ENV: development
    ports:
      - 4000:4000
      - 9229:9229
    ## set your startup file here
    command: node --inspect index.js
